<meta http-equiv="refresh" content="0; url=./index.html">
<script>location.replace("./index.html");</script>

# CHIZEL

Open CHIZEL from the GitHub Pages app entry:

[Launch CHIZEL](./index.html)

If this README appears on the public GitHub Pages site, Pages is serving the fallback Markdown path instead of `index.html`. Push the latest files, then set GitHub Pages to deploy with GitHub Actions and run `Deploy CHIZEL to GitHub Pages`.

The Godot Web export target is:

```text
docs/game/CHIZEL.html
```
